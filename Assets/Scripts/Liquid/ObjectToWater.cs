using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectToWater : MonoBehaviour
{
	private GameObject drop;
	public GameObject spawnee;	
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
	
	/*private void OnTriggerEnter(Collider other)
    {
		
        if (other.tag == "ground")
        {
			Debug.Log("test");
			MeshRenderer MeshComponent = GetComponent<MeshRenderer>();
			MeshComponent.enabled = false;
			drop = Instantiate(spawnee, transform.position, transform.rotation);
				
					
        }
    }
	
	
	private void splitObj(){
		float largeur = transform.scale.x;
		float longueur = transform.scale.y;
		float profondeur = transform.scale.z; 
		
		foreach(){
		
		}
	}
*/}
